package com.logolsp.server

import org.eclipse.lsp4j.services.LanguageClient

/** Marker interface — no extra methods needed beyond the base LanguageClient. */
interface LogoLanguageClient : LanguageClient
